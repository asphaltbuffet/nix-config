{
  "enabledPlugins": {
    "nixd-lsp": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "alejandra -qq /home/grue/nix-config"
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "path=$(jq -r '.file_path // empty'); case \"$path\" in *.nix) nix-instantiate --parse \"$path\" > /dev/null && echo \"✓ Nix syntax OK: $path\" || echo \"✗ Nix syntax error: $path\";; esac"
          }
        ]
      }
    ]
  }
}
