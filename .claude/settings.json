{
  "permissions": {
    "allow": [
      "Bash(jj log:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "path=$(jq -r '.tool_input.file_path // empty'); case \"$path\" in *.nix) nix-instantiate --parse \"$path\" > /dev/null 2>&1 || { echo \"Nix syntax error in $path\" >&2; exit 2; };; esac"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "nixd-lsp": true,
    "github@claude-plugins-official": true
  }
}
